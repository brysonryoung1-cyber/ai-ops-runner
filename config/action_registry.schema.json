{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "OpenClaw action registry",
  "description": "Single source of truth for hostd allowlist, project run allowlist, and UI labels.",
  "type": "object",
  "required": ["actions"],
  "properties": {
    "actions": {
      "type": "array",
      "items": {
        "type": "object",
        "required": ["id", "timeout_sec", "cmd_template"],
        "properties": {
          "id": { "type": "string", "minLength": 1 },
          "label": { "type": "string" },
          "description": { "type": "string" },
          "project_id": { "type": "string", "description": "When set, this action is allowlisted for POST /api/projects/<project_id>/run" },
          "timeout_sec": { "type": "integer", "minimum": 1 },
          "cmd_template": { "type": "string", "description": "Bash command template; ${ROOT_DIR} is substituted by hostd" }
        }
      }
    }
  }
}
