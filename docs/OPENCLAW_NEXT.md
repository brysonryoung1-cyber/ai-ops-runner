# Next Action

Run Soma Phase 0 baseline: trigger **Soma Kajabi Phase 0** from HQ Actions (or hostd). After artifacts are written and `phase0_baseline_status=PASS` is set in `config/project_state.json`, optionally set `gates.allow_orb_backtests=true` to unlock ORB backtest execution.
